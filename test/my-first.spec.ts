describe("Casos de uso", () => {
  it("Caso de uso 1", () => {
    // Arrange
    const a = 1;
    const b = 2;
    const expected = 3;

    // Act
    const result = a + b;

    // Assert
    expect(result).toBe(expected);
  });
});
